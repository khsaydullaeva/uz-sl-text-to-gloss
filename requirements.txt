# NLLB + Qwen text->gloss fine-tuning (reuse .venv-text-gloss-mbart).
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=4.0.0
sacrebleu>=2.4.0
torch>=2.1.0
peft>=0.13.0
trl>=1.0.0
# Word-sense disambiguation (gloss token → correct sign): local sentence
# embeddings, no external API. Default model is multilingual LaBSE.
sentence-transformers>=2.2.0
